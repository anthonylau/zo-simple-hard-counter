version: '2'

services:
  app:
    image: zo-simple-hard-counter
    build: .
    command: node index.js
    ports:
      - 3000:3000
    links:
      - redis
      - postgres
    volumes:
      - .:/usr/src/app

  redis:
    image: redis

  postgres:
    image: postgres:9.2.20-alpine
    ports:
      - 25432:5432

  
